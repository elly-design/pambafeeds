[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# BrowserRouter needs every path to serve index.html so deep links and refreshes work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
